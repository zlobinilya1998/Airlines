import React, { useState } from 'react'

export default function Basket(props){

    let [arr,SetArray] = useState(null)
    
    arr.map((name)=><li>{console.log(name)}</li>)

    return(
        
    )
}